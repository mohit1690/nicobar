package com.nicobar.Pages;

import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.interactions.Actions;

public class MouseOver {

		public static WebDriver driver;

		   public static void main(String[] args) throws InterruptedException {

		       /* driver = new FirefoxDriver();
		        driver.manage().window().maximize();
		        driver.manage().timeouts().implicitlyWait(10, TimeUnit.SECONDS);

		        driver.get("https://www.nicobar.com/catalogue/category/men/shirts_78/?source=plp&currency=INR&category_shop=Men+%3E+Shirts");*/

		     /*   WebElement element = driver.findElement(By.xpath(".//span[@class='result-giftbox-title']"));

		        Actions action = new Actions(driver);

		        action.moveToElement(element).build().perform();

		        driver.findElement(By.xpath("//a[contains(.,'Quick View')]")).click();
		        Thread.sleep(3000);*/
			   
			   //Kapil
			   /*WebElement hover=driver.findElement(By.xpath("));
					   Actions mouse=new Actions(driver);
					   mouse.moveToElement(hover).perform();*/




					   /*Actions builder = new Actions(driver);
					   builder.moveToElement(hoverElement).perform();
					   By locator = By.id("clickElementID");
					   driver.click(locator);*/

		        


	}

}
